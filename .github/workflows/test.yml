name: Test Project

on:
  push:
    branches: ['master']
    paths: ['frontend/**']

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:18
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: 20
      - run: npm run build